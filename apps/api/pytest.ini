[pytest]
pythonpath = .
markers =
    postgres: requires a running Docker daemon (spins up a real Postgres via testcontainers, see tests/test_postgres_specific.py). Skips automatically, doesn't fail, when Docker is unreachable.
